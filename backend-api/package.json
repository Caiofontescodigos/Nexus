{
  "name": "nexus-backend-api",
  "version": "1.0.0",
  "private": true,

  "scripts": {
    "build": "tsc && cp package.json dist/",
    "postbuild": "cp -r node_modules dist/",
    "package": "cd dist && zip -r ../nexus-api.zip . && cd ..",
    "dev": "tsx src/handler.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.9",
    "@types/node": "^22.16.5",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  }
}
